import styled from "@emotion/styled";
import Rating from '@mui/material/Rating';

const CssStars = styled(Rating)({
    '& .MuiRating-iconFilled': {
        color: '#CDE5D7',
      }
})

export default CssStars;